# Phase 7C — URL & Indexation Baseline (pre-remediation)

Taken after Phase 7B (generator-integrity fix), before any Phase 7C source change. Full detail: `URL-INDEXATION-BASELINE.json`.

- Page count: 522. Sitemap URL count: 522 (includes the Phase 7B `reports/phase-7a` contamination).
- **P0 duplicate calculator:** `/calculators/pool-volume-calculator` vs `/calculators/volume-calculator`.
- **MISSING_CANONICAL (16):** all internal tooling (`qa/*`, `reports/*`, `tools/index.html`).
- **IN_SITEMAP_BUT_NOINDEX (32):** 15 internal tooling (`qa/*`, `reports/*`, `search/index.html`) + 16 `reference/datasets/*` (intentionally-noindex developer documentation pages).
- **INTERNAL_TOOLING_PAGE_IS_INDEXABLE (7):** all `audit/google/*.html`, hand-built inline HTML with no `<meta name="robots">` at all.
- **Sitemap contamination:** `reports/phase-7a` (this audit's own dashboard) swept into `sitemap-other.xml` by the Phase 7B build, root-caused to the sitemap generator having no production-content exclusion policy.
- **Legacy chart duplication — true filename-duplicate pairs (2):**
  1. `hot-tub-chemical-levels-chart.html` (root, actively generated by `generate-authority-charts.js`) vs `charts/hot-tub-chemical-levels-chart.html` (static, no generator) → root is authoritative.
  2. `pool-chemical-levels-chart.html` (root, static/orphaned) vs `charts/pool-chemical-levels-chart.html` (static/orphaned) → root is authoritative (higher inbound-link count, and it's the URL baked into the sitewide footer partial).
  - The other 7 root-level chart files and 2 `charts/*.html` files have no counterpart — not duplicates, left as-is.
- **Duplicate calculator URL decision:** `/calculators/pool-volume-calculator` is authoritative — supports 3 pool shapes (rectangular/circular/oval) vs `volume-calculator`'s 2, and matches the sitewide `pool-X-calculator` naming convention every sibling calculator follows.
